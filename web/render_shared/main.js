let cameras = [
    {
        id: 0,
        img_name: "00001",
        width: 1959,
        height: 1090,
        position: [
            -3.0089893469241797, -0.11086489695181866, -3.7527640949141428,
        ],
        rotation: [
            [0.876134201218856, 0.06925962026449776, 0.47706599800804744],
            [-0.04747421839895102, 0.9972110940209488, -0.057586739349882114],
            [-0.4797239414934443, 0.027805376500959853, 0.8769787916452908],
        ],
        fy: 1164.6601287484507,
        fx: 1159.5880733038064,
    },
    {
        id: 1,
        img_name: "00009",
        width: 1959,
        height: 1090,
        position: [
            -2.5199776022057296, -0.09704735754873686, -3.6247725540304545,
        ],
        rotation: [
            [0.9982731285632193, -0.011928707708098955, -0.05751927260507243],
            [0.0065061360949636325, 0.9955928229282383, -0.09355533724430458],
            [0.058381769258182864, 0.09301955098900708, 0.9939511719154457],
        ],
        fy: 1164.6601287484507,
        fx: 1159.5880733038064,
    },
    {
        id: 2,
        img_name: "00017",
        width: 1959,
        height: 1090,
        position: [
            -0.7737533667465242, -0.3364271945329695, -2.9358969417573753,
        ],
        rotation: [
            [0.9998813418672372, 0.013742375651625236, -0.0069605529394208224],
            [-0.014268370388586709, 0.996512943252834, -0.08220929105659476],
            [0.00580653013657589, 0.08229885200307129, 0.9965907801935302],
        ],
        fy: 1164.6601287484507,
        fx: 1159.5880733038064,
    },
    {
        id: 3,
        img_name: "00025",
        width: 1959,
        height: 1090,
        position: [
            1.2198221749590001, -0.2196687861401182, -2.3183162007028453,
        ],
        rotation: [
            [0.9208648867765482, 0.0012010625395201253, 0.389880004297208],
            [-0.06298204172269357, 0.987319521752825, 0.14571693239364383],
            [-0.3847611242348369, -0.1587410451475895, 0.9092635249821667],
        ],
        fy: 1164.6601287484507,
        fx: 1159.5880733038064,
    },
    {
        id: 4,
        img_name: "00033",
        width: 1959,
        height: 1090,
        position: [
            1.742387858893817, -0.13848225198886954, -2.0566370113193146,
        ],
        rotation: [
            [0.24669889292141334, -0.08370189346592856, -0.9654706879349405],
            [0.11343747891376445, 0.9919082664242816, -0.05700815184573074],
            [0.9624300466054861, -0.09545671285663988, 0.2541976029815521],
        ],
        fy: 1164.6601287484507,
        fx: 1159.5880733038064,
    },
    {
        id: 5,
        img_name: "00041",
        width: 1959,
        height: 1090,
        position: [
            3.6567309419223935, -0.16470990600750707, -1.3458085590422042,
        ],
        rotation: [
            [0.2341293058324528, -0.02968330457755884, -0.9717522161434825],
            [0.10270823606832301, 0.99469554638321, -0.005638106875665722],
            [0.9667649592295676, -0.09848690996657204, 0.2359360976431732],
        ],
        fy: 1164.6601287484507,
        fx: 1159.5880733038064,
    },
    {
        id: 6,
        img_name: "00049",
        width: 1959,
        height: 1090,
        position: [
            3.9013554243203497, -0.2597500978038105, -0.8106154188297828,
        ],
        rotation: [
            [0.6717235545638952, -0.015718162115524837, -0.7406351366386528],
            [0.055627354673906296, 0.9980224478387622, 0.029270992841185218],
            [0.7387104058127439, -0.060861588786650656, 0.6712695459756353],
        ],
        fy: 1164.6601287484507,
        fx: 1159.5880733038064,
    },
    {
        id: 7,
        img_name: "00057",
        width: 1959,
        height: 1090,
        position: [4.742994605467533, -0.05591660945412069, 0.9500365976084458],
        rotation: [
            [-0.17042655709210375, 0.01207080756938, -0.9852964448542146],
            [0.1165090336695526, 0.9931575292530063, -0.00798543433078162],
            [0.9784581921120181, -0.1161568667478904, -0.1706667764862097],
        ],
        fy: 1164.6601287484507,
        fx: 1159.5880733038064,
    },
    {
        id: 8,
        img_name: "00065",
        width: 1959,
        height: 1090,
        position: [4.34676307626522, 0.08168160516967145, 1.0876221470355405],
        rotation: [
            [-0.003575447631888379, -0.044792503246552894, -0.9989899137764799],
            [0.10770152645126597, 0.9931680875192705, -0.04491693593046672],
            [0.9941768441149182, -0.10775333677534978, 0.0012732004866391048],
        ],
        fy: 1164.6601287484507,
        fx: 1159.5880733038064,
    },
    {
        id: 9,
        img_name: "00073",
        width: 1959,
        height: 1090,
        position: [3.264984351114202, 0.078974937336732, 1.0117200284114904],
        rotation: [
            [-0.026919994628162257, -0.1565891128261527, -0.9872968974090509],
            [0.08444552208239385, 0.983768234577625, -0.1583319754069128],
            [0.9960643893290491, -0.0876350978794554, -0.013259786205163005],
        ],
        fy: 1164.6601287484507,
        fx: 1159.5880733038064,
    },
];

let camera = cameras[0];

function getProjectionMatrix(fx, fy, width, height) {
    const znear = 0.2;
    const zfar = 200;
    return [
        [(2 * fx) / width, 0, 0, 0],
        [0, -(2 * fy) / height, 0, 0],
        [0, 0, zfar / (zfar - znear), 1],
        [0, 0, -(zfar * znear) / (zfar - znear), 0],
    ].flat();
}

function getViewMatrix(camera) {
    const R = camera.rotation.flat();
    const t = camera.position;
    const camToWorld = [
        [R[0], R[1], R[2], 0],
        [R[3], R[4], R[5], 0],
        [R[6], R[7], R[8], 0],
        [
            -t[0] * R[0] - t[1] * R[3] - t[2] * R[6],
            -t[0] * R[1] - t[1] * R[4] - t[2] * R[7],
            -t[0] * R[2] - t[1] * R[5] - t[2] * R[8],
            1,
        ],
    ].flat();
    return camToWorld;
}
// function translate4(a, x, y, z) {
//     return [
//         ...a.slice(0, 12),
//         a[0] * x + a[4] * y + a[8] * z + a[12],
//         a[1] * x + a[5] * y + a[9] * z + a[13],
//         a[2] * x + a[6] * y + a[10] * z + a[14],
//         a[3] * x + a[7] * y + a[11] * z + a[15],
//     ];
// }

function multiply4(a, b) {
    return [
        b[0] * a[0] + b[1] * a[4] + b[2] * a[8] + b[3] * a[12],
        b[0] * a[1] + b[1] * a[5] + b[2] * a[9] + b[3] * a[13],
        b[0] * a[2] + b[1] * a[6] + b[2] * a[10] + b[3] * a[14],
        b[0] * a[3] + b[1] * a[7] + b[2] * a[11] + b[3] * a[15],
        b[4] * a[0] + b[5] * a[4] + b[6] * a[8] + b[7] * a[12],
        b[4] * a[1] + b[5] * a[5] + b[6] * a[9] + b[7] * a[13],
        b[4] * a[2] + b[5] * a[6] + b[6] * a[10] + b[7] * a[14],
        b[4] * a[3] + b[5] * a[7] + b[6] * a[11] + b[7] * a[15],
        b[8] * a[0] + b[9] * a[4] + b[10] * a[8] + b[11] * a[12],
        b[8] * a[1] + b[9] * a[5] + b[10] * a[9] + b[11] * a[13],
        b[8] * a[2] + b[9] * a[6] + b[10] * a[10] + b[11] * a[14],
        b[8] * a[3] + b[9] * a[7] + b[10] * a[11] + b[11] * a[15],
        b[12] * a[0] + b[13] * a[4] + b[14] * a[8] + b[15] * a[12],
        b[12] * a[1] + b[13] * a[5] + b[14] * a[9] + b[15] * a[13],
        b[12] * a[2] + b[13] * a[6] + b[14] * a[10] + b[15] * a[14],
        b[12] * a[3] + b[13] * a[7] + b[14] * a[11] + b[15] * a[15],
    ];
}

function invert4(a) {
    let b00 = a[0] * a[5] - a[1] * a[4];
    let b01 = a[0] * a[6] - a[2] * a[4];
    let b02 = a[0] * a[7] - a[3] * a[4];
    let b03 = a[1] * a[6] - a[2] * a[5];
    let b04 = a[1] * a[7] - a[3] * a[5];
    let b05 = a[2] * a[7] - a[3] * a[6];
    let b06 = a[8] * a[13] - a[9] * a[12];
    let b07 = a[8] * a[14] - a[10] * a[12];
    let b08 = a[8] * a[15] - a[11] * a[12];
    let b09 = a[9] * a[14] - a[10] * a[13];
    let b10 = a[9] * a[15] - a[11] * a[13];
    let b11 = a[10] * a[15] - a[11] * a[14];
    let det =
        b00 * b11 - b01 * b10 + b02 * b09 + b03 * b08 - b04 * b07 + b05 * b06;
    if (!det) return null;
    return [
        (a[5] * b11 - a[6] * b10 + a[7] * b09) / det,
        (a[2] * b10 - a[1] * b11 - a[3] * b09) / det,
        (a[13] * b05 - a[14] * b04 + a[15] * b03) / det,
        (a[10] * b04 - a[9] * b05 - a[11] * b03) / det,
        (a[6] * b08 - a[4] * b11 - a[7] * b07) / det,
        (a[0] * b11 - a[2] * b08 + a[3] * b07) / det,
        (a[14] * b02 - a[12] * b05 - a[15] * b01) / det,
        (a[8] * b05 - a[10] * b02 + a[11] * b01) / det,
        (a[4] * b10 - a[5] * b08 + a[7] * b06) / det,
        (a[1] * b08 - a[0] * b10 - a[3] * b06) / det,
        (a[12] * b04 - a[13] * b02 + a[15] * b00) / det,
        (a[9] * b02 - a[8] * b04 - a[11] * b00) / det,
        (a[5] * b07 - a[4] * b09 - a[6] * b06) / det,
        (a[0] * b09 - a[1] * b07 + a[2] * b06) / det,
        (a[13] * b01 - a[12] * b03 - a[14] * b00) / det,
        (a[8] * b03 - a[9] * b01 + a[10] * b00) / det,
    ];
}

function rotate4(a, rad, x, y, z) {
    let len = Math.hypot(x, y, z);
    x /= len;
    y /= len;
    z /= len;
    let s = Math.sin(rad);
    let c = Math.cos(rad);
    let t = 1 - c;
    let b00 = x * x * t + c;
    let b01 = y * x * t + z * s;
    let b02 = z * x * t - y * s;
    let b10 = x * y * t - z * s;
    let b11 = y * y * t + c;
    let b12 = z * y * t + x * s;
    let b20 = x * z * t + y * s;
    let b21 = y * z * t - x * s;
    let b22 = z * z * t + c;
    return [
        a[0] * b00 + a[4] * b01 + a[8] * b02,
        a[1] * b00 + a[5] * b01 + a[9] * b02,
        a[2] * b00 + a[6] * b01 + a[10] * b02,
        a[3] * b00 + a[7] * b01 + a[11] * b02,
        a[0] * b10 + a[4] * b11 + a[8] * b12,
        a[1] * b10 + a[5] * b11 + a[9] * b12,
        a[2] * b10 + a[6] * b11 + a[10] * b12,
        a[3] * b10 + a[7] * b11 + a[11] * b12,
        a[0] * b20 + a[4] * b21 + a[8] * b22,
        a[1] * b20 + a[5] * b21 + a[9] * b22,
        a[2] * b20 + a[6] * b21 + a[10] * b22,
        a[3] * b20 + a[7] * b21 + a[11] * b22,
        ...a.slice(12, 16),
    ];
}

function translate4(a, x, y, z) {
    return [
        ...a.slice(0, 12),
        a[0] * x + a[4] * y + a[8] * z + a[12],
        a[1] * x + a[5] * y + a[9] * z + a[13],
        a[2] * x + a[6] * y + a[10] * z + a[14],
        a[3] * x + a[7] * y + a[11] * z + a[15],
    ];
}

function createWorker(self) {

    let buffer;
    let vertexCount = 0;
    let viewProj;
    // 6*4 + 4 + 4 = 8*4
    // XYZ - Position (Float32)
    // XYZ - Scale (Float32)
    // RGBA - colors (uint8)
    // IJKL - quaternion/rot (uint8)
    const rowLength = 3 * 4 + 3 * 4 + 4 + 4; // 32 bytes per vertex
    let lastProj = [];
    let depthIndex = new Uint32Array();
    let lastVertexCount = 0;
    let sortRunning;

    let tmc3Module = null;
    let pendingMobileGS = null;
    const reportProgress = (phase, percent) => {
        postMessage({ progress: { phase, percent } });
    };
    const reportError = (error) => {
        postMessage({
            error: error instanceof Error ? error.message : String(error),
        });
    };

    try {
        // Replace your self.Module inside createWorker with this:
        self.Module = {
            INITIAL_MEMORY: 536870912,
            ALLOW_MEMORY_GROWTH: true,
            locateFile: function(path) {
                if (path.endsWith('.wasm')) return TMC3_URL.replace('.js', '.wasm');
                return path;
            },
            onRuntimeInitialized: async function() {
                tmc3Module = self.Module;
                console.log("TMC3 Decoder Ready!");
                if (pendingMobileGS) {
                    try {
                        reportProgress("decompose", 52);
                        buffer = await processMobileGSData(pendingMobileGS.buffer);
                        postMessage({ buffer: buffer, save: pendingMobileGS.save });
                        throttledSort();
                    } catch(e) {
                        console.error("Delayed Mobile-GS Decode Error:", e);
                        reportError(e);
                    }
                    pendingMobileGS = null;
                }
            }
        };

        importScripts(TMC3_URL); 
    } catch (e) {
        console.error("Failed to load TMC3", e);
    }




    var _floatView = new Float32Array(1);
    var _int32View = new Int32Array(_floatView.buffer);


function normalizeTensorInPlace(v, out) {
    let sum = 0;
    for (let i = 0; i < v.length; i++) sum += v[i] * v[i];
    let mag = Math.sqrt(sum) || 1e-8;
    for (let i = 0; i < v.length; i++) out[i] = v[i] / mag;
}

function contractToUnisphereInPlace(x, y, z, out) {
    let mag = Math.hypot(x, y, z);
    if (mag > 1.0) {
        if (mag === Infinity) {
            x = 0; y = 0; z = 0; // Prevent Infinity * 0 = NaN
        } else {
            let scale = (2.0 - 1.0 / mag) / mag;
            x *= scale;
            y *= scale;
            z *= scale;
        }
    }
    out[0] = x / 4.0 + 0.5;
    out[1] = y / 4.0 + 0.5;
    out[2] = z / 4.0 + 0.5;
}



    function halfToFloat(h) {
        let s = (h & 0x8000) >> 15;
        let e = (h & 0x7C00) >> 10;
        let f = h & 0x03FF;
        if(e === 0) return (s ? -1 : 1) * Math.pow(2, -14) * (f / 1024);
        if(e === 0x1F) return f ? NaN : ((s ? -1 : 1) * Infinity);
        return (s ? -1 : 1) * Math.pow(2, e - 15) * (1 + f / 1024);
    }

    // Expands a 10-bit integer into 30 bits for Morton encoding
    function expandBits(v) {
        v = (v | (v << 16)) & 0x030000ff;
        v = (v | (v <<  8)) & 0x0300f00f;
        v = (v | (v <<  4)) & 0x030c30c3;
        v = (v | (v <<  2)) & 0x09249249;
        return v;
    }

    // Calculates a 3D Morton code from 16-bit inputs (scaled down to 10-bit to fit in 32-bit int)
    function calculateMortonCode(x, y, z) {
        let xx = Math.floor((x / 65535.0) * 1023.0);
        let yy = Math.floor((y / 65535.0) * 1023.0);
        let zz = Math.floor((z / 65535.0) * 1023.0);
        return (expandBits(xx) | (expandBits(yy) << 1) | (expandBits(zz) << 2));
    }

    function floatToHalf(float) {
        _floatView[0] = float;
        var f = _int32View[0];

        var sign = (f >> 31) & 0x0001;
        var exp = (f >> 23) & 0x00ff;
        var frac = f & 0x007fffff;

        var newExp;
        if (exp == 0) {
            newExp = 0;
        } else if (exp < 113) {
            newExp = 0;
            frac |= 0x00800000;
            frac = frac >> (113 - exp);
            if (frac & 0x01000000) {
                newExp = 1;
                frac = 0;
            }
        } else if (exp < 142) {
            newExp = exp - 112;
        } else {
            newExp = 31;
            frac = 0;
        }

        return (sign << 15) | (newExp << 10) | (frac >> 13);
    }

    function packHalf2x16(x, y) {
        return (floatToHalf(x) | (floatToHalf(y) << 16)) >>> 0;
    }

    function generateTexture() {
        if (!buffer) return;
        const f_buffer = new Float32Array(buffer);
        const u_buffer = new Uint8Array(buffer);

        var texwidth = 1024 * 2; // 2048 pixels wide
        var texheight = Math.ceil((2 * vertexCount) / texwidth); // 2 pixels per vertex
        var texdata = new Uint32Array(texwidth * texheight * 4);
        var texdata_c = new Uint8Array(texdata.buffer);
        var texdata_f = new Float32Array(texdata.buffer);

        let texdata_sh = null, texwidth_sh = 1, texheight_sh = 1;

        if (shBuffer) {
            texwidth_sh = 8192; 
            // Each vertex uses 3 pixels (one vec4 per color channel)
            texheight_sh = Math.ceil((vertexCount * 3) / texwidth_sh); 
            texdata_sh = new Float32Array(texwidth_sh * texheight_sh * 4);

            const sh_f_buffer = new Float32Array(shBuffer);
            // Simple copy: sh_f_buffer is already ordered correctly
            texdata_sh.set(sh_f_buffer);
            
            // texwidth_sh = 8192; 
            // const shRowLength = 12; 
            // texheight_sh = Math.ceil((vertexCount * 12) / texwidth_sh); 
            // texdata_sh = new Float32Array(texwidth_sh * texheight_sh * 4);

            // const sh_f_buffer = new Float32Array(shBuffer);

            // for (let i = 0; i < vertexCount; i++) {
            //     for (let j = 0; j < 12; j++) { 
            //         const texX = (i * 12 + j) % texwidth_sh;
            //         const texY = Math.floor((i * 12 + j) / texwidth_sh);
            //         const texIndex = (texY * texwidth_sh + texX) * 4;

            //         if (texIndex < texdata_sh.length) {
            //             texdata_sh[texIndex + 0] = sh_f_buffer[i * 12 + j * 4 + 0]; 
            //             texdata_sh[texIndex + 1] = sh_f_buffer[i * 12 + j * 4 + 1]; 
            //             texdata_sh[texIndex + 2] = sh_f_buffer[i * 12 + j * 4 + 2]; 
            //             texdata_sh[texIndex + 3] = sh_f_buffer[i * 12 + j * 4 + 3]; 
            //         }
            //     }
            // }
        }

        // Here we convert from a .splat file buffer into a texture
        // With a little bit more foresight perhaps this texture file
        // should have been the native format as it'd be very easy to
        // load it into webgl.
        for (let i = 0; i < vertexCount; i++) {
            // x, y, z
            texdata_f[8 * i + 0] = f_buffer[8 * i + 0];
            texdata_f[8 * i + 1] = f_buffer[8 * i + 1];
            texdata_f[8 * i + 2] = f_buffer[8 * i + 2];

            // r, g, b, a (color data at offset 24-27 in buffer)
            texdata_c[4 * (8 * i + 7) + 0] = u_buffer[32 * i + 24 + 0];
            texdata_c[4 * (8 * i + 7) + 1] = u_buffer[32 * i + 24 + 1];
            texdata_c[4 * (8 * i + 7) + 2] = u_buffer[32 * i + 24 + 2];
            texdata_c[4 * (8 * i + 7) + 3] = u_buffer[32 * i + 24 + 3];

            // Scale data at offset 12-23 in buffer (3 float32 values)
            let scale = [
                f_buffer[8 * i + 3],  // offset 12 bytes = 3 * 4 bytes
                f_buffer[8 * i + 4],  // offset 16 bytes = 4 * 4 bytes
                f_buffer[8 * i + 5],  // offset 20 bytes = 5 * 4 bytes
            ];

            // Rotation data at offset 28-31 in buffer (4 uint8 values)
            let rot = [
                (u_buffer[32 * i + 28 + 0] - 128) / 128,
                (u_buffer[32 * i + 28 + 1] - 128) / 128,
                (u_buffer[32 * i + 28 + 2] - 128) / 128,
                (u_buffer[32 * i + 28 + 3] - 128) / 128,
            ];

            // Compute the matrix product of S and R (M = S * R)
            const M = [
                1.0 - 2.0 * (rot[2] * rot[2] + rot[3] * rot[3]),
                2.0 * (rot[1] * rot[2] + rot[0] * rot[3]),
                2.0 * (rot[1] * rot[3] - rot[0] * rot[2]),

                2.0 * (rot[1] * rot[2] - rot[0] * rot[3]),
                1.0 - 2.0 * (rot[1] * rot[1] + rot[3] * rot[3]),
                2.0 * (rot[2] * rot[3] + rot[0] * rot[1]),

                2.0 * (rot[1] * rot[3] + rot[0] * rot[2]),
                2.0 * (rot[2] * rot[3] - rot[0] * rot[1]),
                1.0 - 2.0 * (rot[1] * rot[1] + rot[2] * rot[2]),
            ].map((k, i) => k * scale[Math.floor(i / 3)]);

            const sigma = [
                M[0] * M[0] + M[3] * M[3] + M[6] * M[6],
                M[0] * M[1] + M[3] * M[4] + M[6] * M[7],
                M[0] * M[2] + M[3] * M[5] + M[6] * M[8],
                M[1] * M[1] + M[4] * M[4] + M[7] * M[7],
                M[1] * M[2] + M[4] * M[5] + M[7] * M[8],
                M[2] * M[2] + M[5] * M[5] + M[8] * M[8],
            ];

            // Store covariance data in pixel 1 (same as main.js)
            texdata[8 * i + 4] = packHalf2x16(4 * sigma[0], 4 * sigma[1]);
            texdata[8 * i + 5] = packHalf2x16(4 * sigma[2], 4 * sigma[3]);
            texdata[8 * i + 6] = packHalf2x16(4 * sigma[4], 4 * sigma[5]);
        }

        console.log("Sending texture data: main", texwidth, "x", texheight, ", SH", texwidth_sh, "x", texheight_sh);
        self.postMessage({ texdata, texwidth, texheight }, [texdata.buffer]);
        if (texdata_sh) {
            self.postMessage({ texdata_sh, texwidth_sh, texheight_sh }, [texdata_sh.buffer]);
        }
    }

    function runSort(viewProj) {
        if (!buffer) return;
        const f_buffer = new Float32Array(buffer);
        if (lastVertexCount == vertexCount) {
            let dot =
                lastProj[2] * viewProj[2] +
                lastProj[6] * viewProj[6] +
                lastProj[10] * viewProj[10];
            if (Math.abs(dot - 1) < 0.01) {
                return;
            }
        } else {
            generateTexture();
            lastVertexCount = vertexCount;
        }

        console.time("sort");
        let maxDepth = -Infinity;
        let minDepth = Infinity;
        let sizeList = new Int32Array(vertexCount);
        for (let i = 0; i < vertexCount; i++) {
            let depth =
                ((viewProj[2] * f_buffer[8 * i + 0] +
                    viewProj[6] * f_buffer[8 * i + 1] +
                    viewProj[10] * f_buffer[8 * i + 2]) *
                    4096) |
                0;
            sizeList[i] = depth;
            if (depth > maxDepth) maxDepth = depth;
            if (depth < minDepth) minDepth = depth;
        }

        // This is a 16 bit single-pass counting sort
        let depthInv = (256 * 256 - 1) / (maxDepth - minDepth);
        let counts0 = new Uint32Array(256 * 256);
        for (let i = 0; i < vertexCount; i++) {
            sizeList[i] = ((sizeList[i] - minDepth) * depthInv) | 0;
            counts0[sizeList[i]]++;
        }
        let starts0 = new Uint32Array(256 * 256);
        for (let i = 1; i < 256 * 256; i++)
            starts0[i] = starts0[i - 1] + counts0[i - 1];
        depthIndex = new Uint32Array(vertexCount);
        for (let i = 0; i < vertexCount; i++)
            depthIndex[starts0[sizeList[i]]++] = i;

        console.timeEnd("sort");

        lastProj = viewProj;
        self.postMessage({ depthIndex, viewProj, vertexCount }, [
            depthIndex.buffer,
        ]);
    }

    const throttledSort = () => {
        if (!sortRunning && viewProj) {
            sortRunning = true;
            let lastView = viewProj;
            runSort(lastView);
            setTimeout(() => {
                sortRunning = false;
                if (lastView !== viewProj) throttledSort();
            }, 0);
        }
    };


    function parseMobileGSFile(arrayBuffer) {
        const dataView = new DataView(arrayBuffer);
        const jsonLength = dataView.getUint32(0, true);
        const jsonString = new TextDecoder().decode(new Uint8Array(arrayBuffer, 4, jsonLength));
        const metadata = JSON.parse(jsonString);
        const binaryBaseOffset = 4 + jsonLength;

        function reconstruct(node) {
            if (node && typeof node === 'object') {
                if (node._type === 'ndarray' || node._type === 'bytes') {
                    
                    // Replace the smart offset logic with this:
                    const finalOffset = binaryBaseOffset + node.offset;
                    const alignedBuffer = arrayBuffer.slice(finalOffset, finalOffset + node.length);


                    if (node._type === 'bytes') {
                        return new Uint8Array(alignedBuffer);
                    } else if (node.dtype === 'float16') {
                    const uint16Arr = new Uint16Array(alignedBuffer);
                    const float32Arr = new Float32Array(uint16Arr.length);
                    for(let i=0; i<uint16Arr.length; i++) float32Arr[i] = halfToFloat(uint16Arr[i]);
                    float32Arr.shape = node.shape; // <-- ADD THIS
                    return float32Arr;
                    } else if (node.dtype === 'float32') {
                    const arr = new Float32Array(alignedBuffer);
                    arr.shape = node.shape; // <-- ADD THIS
                        return arr;
                    } else if (node.dtype.includes('int')) {
                        return new Int32Array(alignedBuffer);
                    } else if (node.dtype === 'uint8') {
                        return new Uint8Array(alignedBuffer);
                    }
                } else if (Array.isArray(node)) {
                    return node.map(reconstruct);
                } else {
                    const result = {};
                    for (const key in node) result[key] = reconstruct(node[key]);
                    return result;
                }
            }
            return node;
        }
        return reconstruct(metadata);
    
    }

   function decodeHuffmanBitstream(bitstream, htable) {
        // Build a high-performance binary tree
        const root = { val: null, left: null, right: null };
        
        // htable format from Python dahuffman: { "symbol_str": [bit_length, integer_bit_value] }
        for (let symbolStr in htable) {
            let symbol = parseInt(symbolStr);
            let bitLength = htable[symbolStr][0];
            let bitValue = htable[symbolStr][1];
            
            // Convert integer value to binary string, padded to the correct length
            let code = bitValue.toString(2).padStart(bitLength, '0');
            
            let node = root;
            for (let i = 0; i < code.length; i++) {
                if (code[i] === '0') {
                    if (!node.left) node.left = { val: null, left: null, right: null };
                    node = node.left;
                } else {
                    if (!node.right) node.right = { val: null, left: null, right: null };
                    node = node.right;
                }
            }
            node.val = symbol;
        }
        
        let indices = [];
        let node = root;
        // Decode the bitstream (dahuffman packs Most Significant Bit first)
        for (let i = 0; i < bitstream.length; i++) {
            let byte = bitstream[i];
            for (let b = 7; b >= 0; b--) {
                let bit = (byte >> b) & 1;
                node = bit ? node.right : node.left;
                if (node.val !== null) {
                    indices.push(node.val);
                    node = root; // Reset to root for next code
                }
            }
        }
        return indices;
    }



    function decodeVQAttributesConcat(indexBitstreams, htables, codebookArrays) {
    let chunks = [];
    let totalDim = 0;
    let numPoints = 0;

    // Process each codebook level
    for (let c = 0; c < indexBitstreams.length; c++) {
        let indices = decodeHuffmanBitstream(indexBitstreams[c], htables[c]);
        if (numPoints === 0) numPoints = indices.length;

        // Use the mathematically guaranteed dimension from Python metadata
        let featureDim = codebookArrays[c].shape[1]; 
        
        chunks.push({ indices, codebook: codebookArrays[c], dim: featureDim });
        totalDim += featureDim;
    }

    // FIX 2: Mimic PyTorch torch.cat(..., dim=-1)
    let features = new Float32Array(numPoints * totalDim);
    for (let i = 0; i < numPoints; i++) {
        let currentOffset = 0;
        for (let c = 0; c < chunks.length; c++) {
            let chunk = chunks[c];
            let clusterIdx = chunk.indices[i];
            for (let j = 0; j < chunk.dim; j++) {
                features[i * totalDim + currentOffset + j] = chunk.codebook[clusterIdx * chunk.dim + j];
            }
            currentOffset += chunk.dim;
        }
    }
    return features;
    }


  // Matches Python: contract_to_unisphere (Forward pass only)
function contractToUnisphere(x, y, z) {
    // x, y, z are already in [-1, 1] from your uint16 decoder
    
    // mag = torch.linalg.norm(x, ord=2, dim=-1)
    let mag = Math.hypot(x, y, z);
    
    // mask = mag > 1
    if (mag > 1.0) {
        // x[mask] = (2 - 1 / mag[mask]) * (x[mask] / mag[mask])
        let scale = (2.0 - 1.0 / mag) / mag;
        x *= scale;
        y *= scale;
        z *= scale;
    }
    
    // x = x / 4 + 0.5  # [-inf, inf] is at [0, 1]
    return [
        x / 4.0 + 0.5,
        y / 4.0 + 0.5,
        z / 4.0 + 0.5
    ];
}

   
    // Global shared buffers to prevent Garbage Collection freezing
    // --- 1. HIGH-PERFORMANCE ZERO-ALLOCATION HELPERS ---
    const tcnn_current = new Float32Array(128);
    const tcnn_next = new Float32Array(128);

    function getTCNNFrequencyEncoding(x, y, z, num_freqs, outBuffer) {
        let idx = 0;
        const coords = [x, y, z];


        for (let i = 0; i < 3; i++) {
            let val = coords[i];
            for (let freq = 0; freq < num_freqs; freq++) {
                // 2. CRITICAL: Equivalent to C++ scalbnf(val, log2_frequency) * PI
                let input_val = val * Math.pow(2.0, freq) * Math.PI;

                outBuffer[idx++] = Math.sin(input_val);
                outBuffer[idx++] = Math.cos(input_val); 
            }
        }
        
        const fan_out_encoded = 3 * num_freqs * 2;
        const padded_size = Math.ceil(fan_out_encoded / 16) * 16;
        while (idx < padded_size) {
            outBuffer[idx++] = 1.0;
        }
    }
    function runTCNN_MLP(inputVector, flatParams, inDim, hiddenDim, outDim, numHiddenLayers, activation, outBuffer) {
    const pad16 = (val) => Math.ceil(val / 16) * 16;
    const paddedIn = pad16(inDim);
    const paddedHidden = pad16(hiddenDim);
    const paddedOut = pad16(outDim);
    
    tcnn_current.fill(0);
    tcnn_current.set(inputVector); 
    let offset = 0;

    const applyActivation = (val) => {
        if (activation === 'ReLU') return Math.max(0, val);
        if (activation === 'LeakyReLU') return val > 0 ? val : val * 0.01;
        return val;
    };

    for (let out_idx = 0; out_idx < paddedHidden; out_idx++) {
        let sum = 0;
        for (let in_idx = 0; in_idx < paddedIn; in_idx++) {
            sum += tcnn_current[in_idx] * flatParams[offset + out_idx * paddedIn + in_idx];
        }
        tcnn_next[out_idx] = applyActivation(sum);
    }
    offset += paddedIn * paddedHidden;
    
    // --- HIDDEN LAYERS ---
    for (let layer = 1; layer < numHiddenLayers; layer++) {
        tcnn_current.set(tcnn_next);
        for (let out_idx = 0; out_idx < paddedHidden; out_idx++) {
            let sum = 0;
            for (let in_idx = 0; in_idx < paddedHidden; in_idx++) {
                sum += tcnn_current[in_idx] * flatParams[offset + out_idx * paddedHidden + in_idx];
            }
            tcnn_next[out_idx] = applyActivation(sum);
        }
        offset += paddedHidden * paddedHidden;
    }
    
    // --- OUTPUT LAYER ---
    tcnn_current.set(tcnn_next);
    for (let out_idx = 0; out_idx < paddedOut; out_idx++) {
        let sum = 0;
        for (let in_idx = 0; in_idx < paddedHidden; in_idx++) {
            sum += tcnn_current[in_idx] * flatParams[offset + out_idx * paddedHidden + in_idx];
        }
        tcnn_next[out_idx] = sum; 
    }
    outBuffer.set(tcnn_next.subarray(0, outDim));
}





    function runPyTorch_MLP(inputVector, weights, biases, applyRelu, outBuffer) {
        let outDim = biases.length;
        let inDim = inputVector.length;
        for (let i = 0; i < outDim; i++) {
            let sum = biases[i];
            for (let j = 0; j < inDim; j++) sum += inputVector[j] * weights[i * inDim + j]; 
            outBuffer[i] = applyRelu ? Math.max(0, sum) : sum;
        }
    }

    // --- 2. MAIN DECODE FUNCTION ---
    async function processMobileGSData(arrayBuffer) {
        reportProgress("decompose", 55);
        const save_dict = parseMobileGSFile(arrayBuffer);
        if (!tmc3Module) throw new Error("TMC3 WASM not ready.");

        const fileSystem = tmc3Module.FS || self.FS;
        const mainFunc = tmc3Module.callMain || self.callMain;

        const xyzData = save_dict['xyz'];
        fileSystem.writeFile('/xyz.bin', xyzData);
        
        try {
            console.time("TMC3 Decode");
            mainFunc(['--mode=1', '--compressedStreamPath=/xyz.bin', '--reconstructedDataPath=/xyz.ply']);
            console.timeEnd("TMC3 Decode");
            reportProgress("decompose", 64);
        } catch (err) {
            if (err !== 0 && err.status !== 0 && err.name !== 'ExitStatus') {
                throw new Error("TMC3 failed to decode.");
            }
        }
        
        let plyBytes;
        try {
            plyBytes = fileSystem.readFile('/xyz.ply');
        } finally {
            try { fileSystem.unlink('/xyz.bin'); } catch(e){}
            try { fileSystem.unlink('/xyz.ply'); } catch(e){}
        }

       // 1. Find the end of the header properly
        const headerTextRaw = new TextDecoder().decode(plyBytes.subarray(0, 2000));
        const match = headerTextRaw.match(/end_header\r?\n/);
        if (!match) throw new Error("Could not find end_header in PLY");
        const headerEnd = match.index + match[0].length;
        
        console.log("PLY Header Details:\n", headerTextRaw.substring(0, headerEnd));

        const countMatch = headerTextRaw.substring(0, headerEnd).match(/element vertex (\d+)/);
        vertexCount = countMatch ? parseInt(countMatch[1]) : 0;

        // 2. Safely detect TMC3 data types
        const isUint16 = headerTextRaw.includes("property uint16") || headerTextRaw.includes("property ushort");
        const isInt32 = headerTextRaw.includes("property int32") || headerTextRaw.match(/property int\s+x/);
        const isFloat64 = headerTextRaw.includes("property float64") || headerTextRaw.includes("property double");
        
        const bytesPerVertex = isFloat64 ? 24 : (isUint16 ? 6 : 12);

        let xyz_raw_float = new Float32Array(vertexCount * 3);
        let xyz_int = new Int32Array(vertexCount * 3); // To hold the raw TMC3 integer representations
        let min_x = Infinity, min_y = Infinity, min_z = Infinity;

        const dataView = new DataView(plyBytes.buffer, plyBytes.byteOffset + headerEnd);

        for (let i = 0; i < vertexCount; i++) {
            let x, y, z;
            
            // Read the GPCC decompressed values (which represent integers disguised as your specified type)
            if (isFloat64) {
                x = dataView.getFloat64(i * bytesPerVertex + 0, true);
                y = dataView.getFloat64(i * bytesPerVertex + 8, true);
                z = dataView.getFloat64(i * bytesPerVertex + 16, true);
            } else if (isInt32) {
                x = dataView.getInt32(i * bytesPerVertex + 0, true);
                y = dataView.getInt32(i * bytesPerVertex + 4, true);
                z = dataView.getInt32(i * bytesPerVertex + 8, true);
            } else if (isUint16) {
                x = dataView.getUint16(i * bytesPerVertex + 0, true);
                y = dataView.getUint16(i * bytesPerVertex + 2, true);
                z = dataView.getUint16(i * bytesPerVertex + 4, true);
            } else {
                x = dataView.getFloat32(i * bytesPerVertex + 0, true);
                y = dataView.getFloat32(i * bytesPerVertex + 4, true);
                z = dataView.getFloat32(i * bytesPerVertex + 8, true);
            }

            // Convert to explicit integer (Matches Python's xyz_uint16.int())
            let ix = Math.trunc(x);
            let iy = Math.trunc(y);
            let iz = Math.trunc(z);

            xyz_int[i * 3 + 0] = ix;
            xyz_int[i * 3 + 1] = iy;
            xyz_int[i * 3 + 2] = iz;

            // Track minimums for Python's sort matching: x = x - torch.min(x)
            if (ix < min_x) min_x = ix;
            if (iy < min_y) min_y = iy;
            if (iz < min_z) min_z = iz;

            // Replicate Python's uint16_to_float16: (ix ^ 0x8000) bit-cast to half float
            xyz_raw_float[i * 3 + 0] = halfToFloat((ix ^ 0x8000) & 0xFFFF);
            xyz_raw_float[i * 3 + 1] = halfToFloat((iy ^ 0x8000) & 0xFFFF);
            xyz_raw_float[i * 3 + 2] = halfToFloat((iz ^ 0x8000) & 0xFFFF);
        }
        reportProgress("decompose", 70);

        console.time("Numeric Sort");
        // 3. Subtract Minimums to create normalized integers for sorting
        let xyz_normalized = new Int32Array(vertexCount * 3);
        for (let i = 0; i < vertexCount; i++) {
            xyz_normalized[i * 3 + 0] = xyz_int[i * 3 + 0] - min_x;
            xyz_normalized[i * 3 + 1] = xyz_int[i * 3 + 1] - min_y;
            xyz_normalized[i * 3 + 2] = xyz_int[i * 3 + 2] - min_z;
        }

        // 4. Sort to match Python's calculate_morton_order
        let indicesArray = new Int32Array(vertexCount);
        for (let i = 0; i < vertexCount; i++) indicesArray[i] = i;

        indicesArray.sort((a, b) => {
            // Python's weight is np.power(..., np.arange(3)), which means base^2 for Z.
            // Priority is Z > Y > X
            let az = xyz_normalized[a * 3 + 2], bz = xyz_normalized[b * 3 + 2];
            if (az !== bz) return az - bz;

            let ay = xyz_normalized[a * 3 + 1], by = xyz_normalized[b * 3 + 1];
            if (ay !== by) return ay - by;

            let ax = xyz_normalized[a * 3 + 0], bx = xyz_normalized[b * 3 + 0];
            return ax - bx;
        });
        console.timeEnd("Numeric Sort");
        reportProgress("decompose", 74);

        // 5. Populate the final sorted float array
        let xyz_float = new Float32Array(vertexCount * 3);
        for (let i = 0; i < vertexCount; i++) {
            let old_idx = indicesArray[i];
            xyz_float[i * 3 + 0] = xyz_raw_float[old_idx * 3 + 0];
            xyz_float[i * 3 + 1] = xyz_raw_float[old_idx * 3 + 1];
            xyz_float[i * 3 + 2] = xyz_raw_float[old_idx * 3 + 2];
        }
        console.time("VQ Decode");
        const scale = decodeVQAttributesConcat(save_dict['scale_index'], save_dict['scale_htable'], save_dict['scale_code']);
        const rotation = decodeVQAttributesConcat(save_dict['rotation_index'], save_dict['rotation_htable'], save_dict['rotation_code']);
        const appearance = decodeVQAttributesConcat(save_dict['app_index'], save_dict['app_htable'], save_dict['app_code']);
        console.timeEnd("VQ Decode");
        reportProgress("decompose", 78);

        buffer = new ArrayBuffer(vertexCount * 32);
        const f_buffer = new Float32Array(buffer);
        const u_buffer = new Uint8Array(buffer);
        shBuffer = new ArrayBuffer(vertexCount * 48); 
        const shDataView = new DataView(shBuffer);

        const encoded_xyz = new Float32Array(96);
        const cont_feature = new Float32Array(13);
        const space_feature = new Float32Array(16);
        const view_feature = new Float32Array(16);
        const _opacity = new Float32Array(1);
        const _features_dc = new Float32Array(3);
        const _features_rest = new Float32Array(9);

        const feat1 = new Float32Array(save_dict['MLP_offset']['main.0.bias'].length);
        const feat2 = new Float32Array(save_dict['MLP_offset']['main.2.bias'].length);
        const feat3 = new Float32Array(save_dict['MLP_offset']['main.4.bias'].length);
        const sh_offset = new Float32Array(save_dict['MLP_offset']['shs_output.0.bias'].length);

        // NEW STATIC BUFFERS
        const uni_coords = new Float32Array(3);
        const shs_flat = new Float32Array(12);
        const shs_norm = new Float32Array(12);
        const scales_raw = new Float32Array(3);
        const scales_norm = new Float32Array(3);
        const rot_raw = new Float32Array(4);
        const shsnn_input = new Float32Array(23); // 12 + 1 + 3 + 3 + 4 = 23
        const sh_f_buffer = new Float32Array(shBuffer); // Direct array view (Drops DataView overhead)
        

        console.log("RAW XYZ Point 0:", xyz_float[0], xyz_float[1], xyz_float[2]);
        console.log("RAW Scale Point 0:", scale[0], scale[1], scale[2]);
        console.log("RAW App Point 0:", appearance[0], appearance[1], appearance[2]);

        const sigmoid = (x) => 1 / (1 + Math.exp(-x));
        console.time("Neural Decode Loop");
        const progressStep = Math.max(1, Math.floor(vertexCount / 80));
        for (let i = 0; i < vertexCount; i++) {
            if (i % progressStep === 0) {
                reportProgress("decompose", 78 + (i / vertexCount) * 17);
            }
            let x = xyz_float[i*3], y = xyz_float[i*3+1], z = xyz_float[i*3+2];
            
            // Manual indexing avoids `.subarray()` object creation
            const appDim = appearance.length / vertexCount; 

            let app0 = appearance[i * appDim + 0], app1 = appearance[i * appDim + 1], app2 = appearance[i * appDim + 2];
            let app3 = appearance[i * appDim + 3], app4 = appearance[i * appDim + 4], app5 = appearance[i * appDim + 5];

            contractToUnisphereInPlace(x, y, z, uni_coords);

            getTCNNFrequencyEncoding(uni_coords[0], uni_coords[1], uni_coords[2], 16, encoded_xyz); 
            runTCNN_MLP(encoded_xyz, save_dict['MLP_cont'], 96, 64, 13, 1, 'ReLU', cont_feature);
            
            space_feature.set(cont_feature);
            space_feature[13] = app0; space_feature[14] = app1; space_feature[15] = app2;
            
            view_feature.set(cont_feature);
            view_feature[13] = app3; view_feature[14] = app4; view_feature[15] = app5;

            runTCNN_MLP(space_feature, save_dict['MLP_opacity'], 16, 64, 1, 1, 'LeakyReLU', _opacity);
            runTCNN_MLP(space_feature, save_dict['MLP_dc'], 16, 64, 3, 1, 'LeakyReLU', _features_dc);
            runTCNN_MLP(view_feature, save_dict['MLP_sh'], 16, 64, 9, 1, 'LeakyReLU', _features_rest); 

            
            // save_dict['MLP_cont'] = null;
            // save_dict['MLP_opacity'] = null;
            // save_dict['MLP_sh'] = null;
            // save_dict['MLP_offset'] = null;
            
            // Reusing static buffers for PyTorch MLP
            shs_flat[0] = _features_dc[0]; shs_flat[1] = _features_dc[1]; shs_flat[2] = _features_dc[2];
            for(let j=0; j<9; j++) shs_flat[3+j] = _features_rest[j];
            normalizeTensorInPlace(shs_flat, shs_norm); 

            let act_opacity = sigmoid(_opacity[0]);
            
            let act_scale_x = Math.exp(scale[i*3]);
            let act_scale_y = Math.exp(scale[i*3+1]);
            let act_scale_z = Math.exp(scale[i*3+2]);
            scales_raw[0] = act_scale_x; scales_raw[1] = act_scale_y; scales_raw[2] = act_scale_z;
            normalizeTensorInPlace(scales_raw, scales_norm); 
            
            let r0 = rotation[i*4], r1 = rotation[i*4+1], r2 = rotation[i*4+2], r3 = rotation[i*4+3];
            let rot_mag = Math.hypot(r0, r1, r2, r3) || 1e-8;
            let act_rot_0 = r0 / rot_mag;
            let act_rot_1 = r1 / rot_mag;
            let act_rot_2 = r2 / rot_mag;
            let act_rot_3 = r3 / rot_mag;

            shsnn_input.set(shs_norm, 0);       
            shsnn_input[12] = act_opacity;      
            shsnn_input.set(scales_norm, 13);   
            
            shsnn_input[16] = x; shsnn_input[17] = y; shsnn_input[18] = z;
            shsnn_input[19] = act_rot_0; shsnn_input[20] = act_rot_1; 
            shsnn_input[21] = act_rot_2; shsnn_input[22] = act_rot_3;


            runPyTorch_MLP(shsnn_input, save_dict['MLP_offset']['main.0.weight'], save_dict['MLP_offset']['main.0.bias'], true, feat1);
            runPyTorch_MLP(feat1, save_dict['MLP_offset']['main.2.weight'], save_dict['MLP_offset']['main.2.bias'], true, feat2);
            runPyTorch_MLP(feat2, save_dict['MLP_offset']['main.4.weight'], save_dict['MLP_offset']['main.4.bias'], true, feat3);
            runPyTorch_MLP(feat3, save_dict['MLP_offset']['shs_output.0.weight'], save_dict['MLP_offset']['shs_output.0.bias'], false, sh_offset);


            _features_dc[0] += sh_offset[0]; 
            _features_dc[1] += sh_offset[1]; 
            _features_dc[2] += sh_offset[2];
            for(let r = 0; r < 3; r++) {
                _features_rest[r * 3 + 0] += sh_offset[(r + 1) * 3 + 0];
                _features_rest[r * 3 + 1] += sh_offset[(r + 1) * 3 + 1];
                _features_rest[r * 3 + 2] += sh_offset[(r + 1) * 3 + 2];
            }

            
            // Pack Position
            f_buffer[8 * i + 0] = x;
            f_buffer[8 * i + 1] = y;
            f_buffer[8 * i + 2] = z;

            f_buffer[8 * i + 3] = act_scale_x;
            f_buffer[8 * i + 4] = act_scale_y;
            f_buffer[8 * i + 5] = act_scale_z;

            const SH_C0 = 0.28209479177387814;
            u_buffer[32 * i + 24 + 0] = Math.max(0, Math.min(255, (_features_dc[0] * SH_C0 + 0.5) * 255));
            u_buffer[32 * i + 24 + 1] = Math.max(0, Math.min(255, (_features_dc[1] * SH_C0 + 0.5) * 255));
            u_buffer[32 * i + 24 + 2] = Math.max(0, Math.min(255, (_features_dc[2] * SH_C0 + 0.5) * 255));
            
            u_buffer[32 * i + 24 + 3] = Math.max(0, Math.min(255, act_opacity * 255));

            u_buffer[32 * i + 28 + 0] = Math.max(0, Math.min(255, act_rot_0 * 128 + 128));
            u_buffer[32 * i + 28 + 1] = Math.max(0, Math.min(255, act_rot_1 * 128 + 128));
            u_buffer[32 * i + 28 + 2] = Math.max(0, Math.min(255, act_rot_2 * 128 + 128));
            u_buffer[32 * i + 28 + 3] = Math.max(0, Math.min(255, act_rot_3 * 128 + 128));

 
            const shOff = i * 12; 

            sh_f_buffer[shOff + 0]  = _features_dc[0]; 
            sh_f_buffer[shOff + 1]  = _features_rest[0]; // Coeff1_R
            sh_f_buffer[shOff + 2]  = _features_rest[3]; // Coeff2_R
            sh_f_buffer[shOff + 3]  = _features_rest[6]; // Coeff3_R

            sh_f_buffer[shOff + 4]  = _features_dc[1]; 
            sh_f_buffer[shOff + 5]  = _features_rest[1]; // Coeff1_G
            sh_f_buffer[shOff + 6]  = _features_rest[4]; // Coeff2_G
            sh_f_buffer[shOff + 7]  = _features_rest[7]; // Coeff3_G

            sh_f_buffer[shOff + 8]  = _features_dc[2]; 
            sh_f_buffer[shOff + 9]  = _features_rest[2]; // Coeff1_B
            sh_f_buffer[shOff + 10] = _features_rest[5]; // Coeff2_B
            sh_f_buffer[shOff + 11] = _features_rest[8]; // Coeff3_B
            
            

        //    if (i === 0) {
        //     console.log("\n--- Vertex 0 Comparison (JS) ---");
        //     console.log("1. Uni Coords:", Array.from(uni_coords).map(n => n.toFixed(4)));
            
        //     console.log("2. cont_feature (JS):", Array.from(cont_feature).map(n => n.toFixed(4)));
            
        //     console.log("3. space_feature (JS):", Array.from(space_feature).map(n => n.toFixed(4)));
            
        //     console.log("4. Appearance[0-2]:", app0.toFixed(4), app1.toFixed(4), app2.toFixed(4));
            
        //     console.log("5. DC (Pre-offset):", _features_dc[0].toFixed(4), _features_dc[1].toFixed(4), _features_dc[2].toFixed(4));
        // }



        }
          
     
        console.timeEnd("Neural Decode Loop");
        reportProgress("decompose", 95);
        Object.keys(save_dict).forEach(key => delete save_dict[key]);
        return buffer;
    }


    
    let shBuffer = null;
    self.onmessage = async (e) => {
        if (e.data.mobilegs) {
            if (!tmc3Module) {
                console.log("WASM not ready yet. Queuing payload...");
                pendingMobileGS = { buffer: e.data.mobilegs, save: !!e.data.save };
            } else {
                try {
                    reportProgress("decompose", 52);
                    buffer = await processMobileGSData(e.data.mobilegs);
                    postMessage({ buffer: buffer, save: !!e.data.save });
                    throttledSort();
                } catch (err) {
                    console.error("Mobile-GS Decode Error:", err);
                    reportError(err);
                }
            }
        } else if (e.data.buffer) {
            buffer = e.data.buffer;
            vertexCount = e.data.vertexCount;
        } else if (e.data.view) {
            viewProj = e.data.view;
            throttledSort();
        }
    };
}
const vertexShaderSource = `
#version 300 es
precision highp float;
precision highp int;

uniform highp usampler2D u_texture;
uniform highp sampler2D u_sh_texture;
uniform mat4 projection, view;
uniform vec2 focal;
uniform vec2 viewport;
uniform vec3 camPos;

in vec2 position;
in int index;

out vec4 vColor;
out vec2 vPosition;

const float SH_C0 = 0.28209479177387814;
const float SH_C1 = 0.4886025119029199;
const float SH_C2[5] = float[5](1.0925484305920792, -1.0925484305920792, 0.31539156525252005, -1.0925484305920792, 0.5462742152960396);
const float SH_C3[7] = float[7](-0.5900435899266435, 2.890611442640554, -0.4570457994644658, 0.3731763325901154, -0.4570457994644658, 1.445305721320277, -0.5900435899266435);



float computeSHChannel(vec3 dir, int channel, int vertexIndex) {
    int texWidth = 8192; 
    
    // Each vertex has 3 pixels in the texture: R, G, and B.
    int pixelIndex = vertexIndex * 3 + channel;
    ivec2 texCoord = ivec2(pixelIndex % texWidth, pixelIndex / texWidth);
    
    // Fetch one vec4 containing (DC, Rest1, Rest2, Rest3)
    vec4 sh = texelFetch(u_sh_texture, texCoord, 0);

    float x = dir.x, y = dir.y, z = dir.z;
    
    // Degree 0 (DC)
    float result = SH_C0 * sh.x;

    // Degree 1 (First Order)
    // The standard 3DGS formula for 1st order SH:
    result = result - SH_C1 * y * sh.y + SH_C1 * z * sh.z - SH_C1 * x * sh.w;

    return clamp(result + 0.5, 0.0, 1.0);
}



// float computeSHChannel(vec3 dir, int channel, int vertexIndex) {
//     int texWidth = 8192; 

//     float sh[12];
//     for (int i = 0; i < 12; i++) {
//         int globalPixelIndex = i / 4;

//         int texX = (vertexIndex * 12 + globalPixelIndex) % texWidth;
//         int texY = (vertexIndex * 12 + globalPixelIndex) / texWidth;

//         ivec2 texCoord = ivec2(texX, texY);
//         vec4 pixel = texelFetch(u_sh_texture, texCoord, 0);

//         int channelInPixel = i % 4;
//         sh[i] = pixel[channelInPixel];
//     }

//     float shCoeffs[4];

//     if (channel == 0) {
//         shCoeffs[0] = sh[0];   

//         shCoeffs[1] = sh[3];   
//         shCoeffs[2] = sh[4];  
//         shCoeffs[3] = sh[5];   

 
//     } else if (channel == 1) {
//         shCoeffs[0] = sh[1];   

//         shCoeffs[1] = sh[18];  
//         shCoeffs[2] = sh[19];  
//         shCoeffs[3] = sh[20];  

//     } else {
//         shCoeffs[0] = sh[2];   

//         shCoeffs[1] = sh[33];  
//         shCoeffs[2] = sh[34];  
//         shCoeffs[3] = sh[35];  


//     }

//     float x = dir.x, y = dir.y, z = dir.z;
//     float result = SH_C0 * shCoeffs[0];

//     result = result - SH_C1 * y * shCoeffs[1] + SH_C1 * z * shCoeffs[2] - SH_C1 * x * shCoeffs[3];

   
//     return max(result+0.5, 0.0);
// }

void main() {
    uvec4 cen = texelFetch(u_texture, ivec2((uint(index) & 0x3ffu) << 1, uint(index) >> 10), 0);
    vec4 cam = view * vec4(uintBitsToFloat(cen.xyz), 1);
    vec4 pos2d = projection * cam;

    float clip = 1.2 * pos2d.w;
    if (pos2d.z < -clip || pos2d.x < -clip || pos2d.x > clip || pos2d.y < -clip || pos2d.y > clip) {
        gl_Position = vec4(0.0, 0.0, 2.0, 1.0);
        return;
    }

    uvec4 cov = texelFetch(u_texture, ivec2(((uint(index) & 0x3ffu) << 1) | 1u, uint(index) >> 10), 0);
    vec2 u1 = unpackHalf2x16(cov.x), u2 = unpackHalf2x16(cov.y), u3 = unpackHalf2x16(cov.z);
    mat3 Vrk = mat3(u1.x, u1.y, u2.x, u1.y, u2.y, u3.x, u2.x, u3.x, u3.y);

    mat3 J = mat3(
        focal.x / cam.z, 0., -(focal.x * cam.x) / (cam.z * cam.z),
        0., -focal.y / cam.z, (focal.y * cam.y) / (cam.z * cam.z),
        0., 0., 0.
    );

    mat3 T = transpose(mat3(view)) * J;
    mat3 cov2d = transpose(T) * Vrk * T;

    float mid = (cov2d[0][0] + cov2d[1][1]) / 2.0;
    float radius = length(vec2((cov2d[0][0] - cov2d[1][1]) / 2.0, cov2d[0][1]));
    float lambda1 = mid + radius, lambda2 = mid - radius;

    if(lambda2 < 0.0) return;
    vec2 diagonalVector = normalize(vec2(cov2d[0][1], lambda1 - cov2d[0][0]));
    vec2 majorAxis = min(sqrt(2.0 * lambda1), 1024.0) * diagonalVector;
    vec2 minorAxis = min(sqrt(2.0 * lambda2), 1024.0) * vec2(diagonalVector.y, -diagonalVector.x);

    vec3 center_world = uintBitsToFloat(cen.xyz);
    vec3 view_dir = normalize(center_world - camPos);
    
    float r = computeSHChannel(view_dir, 0, index);
    float g = computeSHChannel(view_dir, 1, index);
    float b = computeSHChannel(view_dir, 2, index);

    float alpha = float((cov.w >> 24) & 0xffu) / 255.0;
    vColor = vec4(r, g, b, alpha);
    vPosition = position;

    vec2 vCenter = vec2(pos2d) / pos2d.w;
    gl_Position = vec4(
        vCenter
        + position.x * majorAxis / viewport
        + position.y * minorAxis / viewport, 0.0, 1.0);

}
`.trim();

const fragmentShaderSource = `
#version 300 es
precision highp float;

in vec4 vColor;
in vec2 vPosition;

out vec4 fragColor;

void main () {
    float A = -dot(vPosition, vPosition);
    if (A < -4.0) discard;
    float B = exp(A) * vColor.a;
    fragColor = vec4(B * vColor.rgb, B);
}

`.trim();

let defaultViewMatrix = [
    0.47, 0.04, 0.88, 0, -0.11, 0.99, 0.02, 0, -0.88, -0.11, 0.47, 0, 0.07,
    0.03, 6.55, 1,
];
let viewMatrix = defaultViewMatrix;
function ensureViewerDom() {
    if (location.host.includes("hf.space")) document.body.classList.add("nohf");
    if (document.getElementById("canvas")) return;

    document.body.insertAdjacentHTML(
        "beforeend",
        `
        <div id="info">
            <h3 class="nohf">Flux-GS</h3>
        </div>
        <div id="progress-panel">
            <div id="progress-label">Loading model 0%</div>
            <div id="progress-track">
                <div id="progress"></div>
            </div>
        </div>
        <div id="message"></div>
        <div class="scene" id="spinner">
            <div class="cube-wrapper">
                <div class="cube">
                    <div class="cube-faces">
                        <div class="cube-face bottom"></div>
                        <div class="cube-face top"></div>
                        <div class="cube-face left"></div>
                        <div class="cube-face right"></div>
                        <div class="cube-face back"></div>
                        <div class="cube-face front"></div>
                    </div>
                </div>
            </div>
        </div>
        <canvas id="canvas"></canvas>
        <div id="quality">
            <span id="fps"></span>
        </div>
        <div id="caminfo">
            <span id="camid"></span>
        </div>
        `,
    );
}

async function main() {






    let carousel = true;
    const progressPanel = document.getElementById("progress-panel");
    const progress = document.getElementById("progress");
    const progressLabel = document.getElementById("progress-label");
    let progressVisible = false;
    const progressText = {
        load: "Loading model",
        decompose: "Decomposing model",
        prepare: "Preparing model",
    };
    const showProgress = (percent, label = progressText.load) => {
        if (!progress) return;
        progressVisible = true;
        const clamped = Math.max(0, Math.min(100, percent));
        if (progressPanel) progressPanel.style.display = "block";
        progress.style.width = clamped + "%";
        if (progressLabel) {
            progressLabel.innerText = label + " " + Math.round(clamped) + "%";
        }
    };
    const hideProgress = () => {
        if (!progress) return;
        progressVisible = false;
        progress.style.width = "100%";
        setTimeout(() => {
            if (!progressVisible) {
                if (progressPanel) progressPanel.style.display = "none";
                progress.style.width = "0%";
            }
        }, 200);
    };

    showProgress(0);
    const params = new URLSearchParams(location.search);
    try {
        viewMatrix = JSON.parse(decodeURIComponent(location.hash.slice(1)));
        carousel = false;
    } catch (err) { }
    const viewerConfig = window.FLUX_GS_CONFIG || {};
    const modelBaseUrl =
        viewerConfig.modelBaseUrl ||
        "https://huggingface.co/datasets/mobile-gs2/mobile-gs2/resolve/main/";
    const defaultModel = viewerConfig.defaultModel || "garden.json";
    const url = new URL(
        params.get("url") || defaultModel,
        modelBaseUrl,
        // "comp.json",
        // location.href,
    );

    const req = await fetch(url, {
        mode: "cors",
        credentials: "omit",
    });
    if (req.status != 200)
        throw new Error(req.status + " Unable to load " + req.url);

    const rowLength = 3 * 4 + 3 * 4 + 4 + 4;
    const reader = req.body.getReader();
    let contentLength = parseInt(req.headers.get("content-length") || "0", 10);
    let splatData = new Uint8Array(contentLength);

    const downsample =
        splatData.length / rowLength > 500000 ? 1 : 1 / devicePixelRatio;
    console.log(splatData.length / rowLength, downsample);


        // Locate this in your main() function:
    const tmc3Path = new URL("../tools/tmc3.js", window.location.href).href;

    const worker = new Worker(
        URL.createObjectURL(
            new Blob([`const TMC3_URL = "${tmc3Path}";\n(`, createWorker.toString(), ")(self)"], {
                type: "application/javascript",
            }),
        ),
    );
    // const worker = new Worker(
    //     URL.createObjectURL(
    //         new Blob(["(", createWorker.toString(), ")(self)"], {
    //             type: "application/javascript",
    //         }),
    //     ),
    // );

    const canvas = document.getElementById("canvas");
    const fps = document.getElementById("fps");
    const camid = document.getElementById("camid");

    let projectionMatrix;

    const gl = canvas.getContext("webgl2", {
        antialias: false,
    });

    const vertexShader = gl.createShader(gl.VERTEX_SHADER);
    gl.shaderSource(vertexShader, vertexShaderSource);
    gl.compileShader(vertexShader);
    if (!gl.getShaderParameter(vertexShader, gl.COMPILE_STATUS))
        console.error(gl.getShaderInfoLog(vertexShader));

    const fragmentShader = gl.createShader(gl.FRAGMENT_SHADER);
    gl.shaderSource(fragmentShader, fragmentShaderSource);
    gl.compileShader(fragmentShader);
    if (!gl.getShaderParameter(fragmentShader, gl.COMPILE_STATUS))
        console.error(gl.getShaderInfoLog(fragmentShader));

    const program = gl.createProgram();
    gl.attachShader(program, vertexShader);
    gl.attachShader(program, fragmentShader);
    gl.linkProgram(program);
    gl.useProgram(program);

    if (!gl.getProgramParameter(program, gl.LINK_STATUS))
        console.error(gl.getProgramInfoLog(program));

    gl.disable(gl.DEPTH_TEST);
    gl.enable(gl.BLEND);
    gl.blendFuncSeparate(
        gl.ONE_MINUS_DST_ALPHA,
        gl.ONE,
        gl.ONE_MINUS_DST_ALPHA,
        gl.ONE,
    );
    gl.blendEquationSeparate(gl.FUNC_ADD, gl.FUNC_ADD);

    const u_projection = gl.getUniformLocation(program, "projection");
    const u_viewport = gl.getUniformLocation(program, "viewport");
    const u_focal = gl.getUniformLocation(program, "focal");
    const u_view = gl.getUniformLocation(program, "view");
    const u_camPos = gl.getUniformLocation(program, "camPos"); 

    // positions
    const triangleVertices = new Float32Array([-2, -2, 2, -2, 2, 2, -2, 2]);
    const vertexBuffer = gl.createBuffer();
    gl.bindBuffer(gl.ARRAY_BUFFER, vertexBuffer);
    gl.bufferData(gl.ARRAY_BUFFER, triangleVertices, gl.STATIC_DRAW);
    const a_position = gl.getAttribLocation(program, "position");
    gl.enableVertexAttribArray(a_position);
    gl.bindBuffer(gl.ARRAY_BUFFER, vertexBuffer);
    gl.vertexAttribPointer(a_position, 2, gl.FLOAT, false, 0, 0);

    var mainTexture = gl.createTexture();
    gl.bindTexture(gl.TEXTURE_2D, mainTexture);
    gl.texImage2D(gl.TEXTURE_2D, 0, gl.RGBA32UI, 1, 1, 0, gl.RGBA_INTEGER, gl.UNSIGNED_INT, new Uint32Array([0, 0, 0, 0]));
    gl.texParameteri(gl.TEXTURE_2D, gl.TEXTURE_WRAP_S, gl.CLAMP_TO_EDGE);
    gl.texParameteri(gl.TEXTURE_2D, gl.TEXTURE_WRAP_T, gl.CLAMP_TO_EDGE);
    gl.texParameteri(gl.TEXTURE_2D, gl.TEXTURE_MIN_FILTER, gl.NEAREST);
    gl.texParameteri(gl.TEXTURE_2D, gl.TEXTURE_MAG_FILTER, gl.NEAREST);
    var u_textureLocation = gl.getUniformLocation(program, "u_texture");
    gl.uniform1i(u_textureLocation, 0);

    var shTexture = gl.createTexture();
    gl.bindTexture(gl.TEXTURE_2D, shTexture);
    gl.texImage2D(gl.TEXTURE_2D, 0, gl.RGBA32F, 1, 1, 0, gl.RGBA, gl.FLOAT, new Float32Array([0, 0, 0, 0]));
    gl.texParameteri(gl.TEXTURE_2D, gl.TEXTURE_WRAP_S, gl.CLAMP_TO_EDGE);
    gl.texParameteri(gl.TEXTURE_2D, gl.TEXTURE_WRAP_T, gl.CLAMP_TO_EDGE);
    gl.texParameteri(gl.TEXTURE_2D, gl.TEXTURE_MIN_FILTER, gl.NEAREST);
    gl.texParameteri(gl.TEXTURE_2D, gl.TEXTURE_MAG_FILTER, gl.NEAREST);
    var u_shTextureLocation = gl.getUniformLocation(program, "u_sh_texture");
    gl.uniform1i(u_shTextureLocation, 1);

    const indexBuffer = gl.createBuffer();
    const a_index = gl.getAttribLocation(program, "index");
    gl.enableVertexAttribArray(a_index);
    gl.bindBuffer(gl.ARRAY_BUFFER, indexBuffer);
    gl.vertexAttribIPointer(a_index, 1, gl.INT, false, 0, 0);
    gl.vertexAttribDivisor(a_index, 1);

    const resize = () => {
        gl.uniform2fv(u_focal, new Float32Array([camera.fx, camera.fy]));

        projectionMatrix = getProjectionMatrix(
            camera.fx,
            camera.fy,
            innerWidth,
            innerHeight,
        );

        gl.uniform2fv(u_viewport, new Float32Array([innerWidth, innerHeight]));

        gl.canvas.width = Math.round(innerWidth / downsample);
        gl.canvas.height = Math.round(innerHeight / downsample);
        gl.viewport(0, 0, gl.canvas.width, gl.canvas.height);

        gl.uniformMatrix4fv(u_projection, false, projectionMatrix);
    };

    window.addEventListener("resize", resize);
    resize();

    const showWorkerError = (error) => {
        progressVisible = false;
        if (progressPanel) progressPanel.style.display = "none";
        document.getElementById("spinner").style.display = "none";
        document.getElementById("message").innerText =
            "Model decompression failed: " + error;
    };

    worker.onerror = (event) => {
        showWorkerError(event.message || "Unknown worker error");
    };

    worker.onmessage = (e) => {
        if (e.data.error) {
            showWorkerError(e.data.error);
        } else if (e.data.progress) {
            showProgress(
                e.data.progress.percent,
                progressText[e.data.progress.phase] || progressText.prepare,
            );
        } else if (e.data.buffer) {
            showProgress(96, progressText.prepare);
            splatData = new Uint8Array(e.data.buffer);
            if (e.data.save) {
                const blob = new Blob([splatData.buffer], {
                    type: "application/octet-stream",
                });
                const link = document.createElement("a");
                link.download = "model.splat";
                link.href = URL.createObjectURL(blob);
                document.body.appendChild(link);
                link.click();
            }
        } else if (e.data.texdata) {
            showProgress(98, progressText.prepare);
            const { texdata, texwidth, texheight } = e.data;
            console.log("Received main texture data:", texwidth, "x", texheight);
            gl.activeTexture(gl.TEXTURE0);
            gl.bindTexture(gl.TEXTURE_2D, mainTexture);
            gl.texParameteri(
                gl.TEXTURE_2D,
                gl.TEXTURE_WRAP_S,
                gl.CLAMP_TO_EDGE,
            );
            gl.texParameteri(
                gl.TEXTURE_2D,
                gl.TEXTURE_WRAP_T,
                gl.CLAMP_TO_EDGE,
            );
            gl.texParameteri(gl.TEXTURE_2D, gl.TEXTURE_MIN_FILTER, gl.NEAREST);
            gl.texParameteri(gl.TEXTURE_2D, gl.TEXTURE_MAG_FILTER, gl.NEAREST);

            gl.texImage2D(
                gl.TEXTURE_2D,
                0,
                gl.RGBA32UI,
                texwidth,
                texheight,
                0,
                gl.RGBA_INTEGER,
                gl.UNSIGNED_INT,
                texdata,
            );
            console.log("Main texture updated successfully");
        } else if (e.data.texdata_sh) {
            showProgress(99, progressText.prepare);
            const { texdata_sh, texwidth_sh, texheight_sh } = e.data;
            console.log("Received SH texture data:", texwidth_sh, "x", texheight_sh);
            gl.activeTexture(gl.TEXTURE1);
            gl.bindTexture(gl.TEXTURE_2D, shTexture);
            gl.texParameteri(gl.TEXTURE_2D, gl.TEXTURE_WRAP_S, gl.CLAMP_TO_EDGE);
            gl.texParameteri(gl.TEXTURE_2D, gl.TEXTURE_WRAP_T, gl.CLAMP_TO_EDGE);
            gl.texParameteri(gl.TEXTURE_2D, gl.TEXTURE_MIN_FILTER, gl.NEAREST);
            gl.texParameteri(gl.TEXTURE_2D, gl.TEXTURE_MAG_FILTER, gl.NEAREST);
            gl.texImage2D(gl.TEXTURE_2D, 0, gl.RGBA32F, texwidth_sh, texheight_sh, 0, gl.RGBA, gl.FLOAT, texdata_sh);
            console.log("SH texture updated successfully");
        } else if (e.data.depthIndex) {
            const { depthIndex, viewProj } = e.data;
            gl.bindBuffer(gl.ARRAY_BUFFER, indexBuffer);
            gl.bufferData(gl.ARRAY_BUFFER, depthIndex, gl.DYNAMIC_DRAW);
            vertexCount = e.data.vertexCount;
            hideProgress();
        }
    };

    let activeKeys = [];
    let currentCameraIndex = 0;

    window.addEventListener("keydown", (e) => {
        // if (document.activeElement != document.body) return;
        carousel = false;
        if (!activeKeys.includes(e.code)) activeKeys.push(e.code);
        // 1. Fix the digit check to strictly match single numbers (0-9)
        if (/^\d$/.test(e.key)) {
            let newIndex = parseInt(e.key);
            // Ensure the parsed index is within the bounds of your array
            if (newIndex >= 0 && newIndex < cameras.length) {
                currentCameraIndex = newIndex;
                camera = cameras[currentCameraIndex];
                viewMatrix = getViewMatrix(camera);
            }
        }
        
        // 2. Keep the global 'camera' reference updated when cycling
        if (["-", "_"].includes(e.key)) {
            currentCameraIndex = (currentCameraIndex + cameras.length - 1) % cameras.length;
            camera = cameras[currentCameraIndex]; // Now updating global reference
            viewMatrix = getViewMatrix(camera);
        }
        
        if (["+", "="].includes(e.key)) {
            currentCameraIndex = (currentCameraIndex + 1) % cameras.length;
            camera = cameras[currentCameraIndex]; // Now updating global reference
            viewMatrix = getViewMatrix(camera);
        }
        camid.innerText = "cam  " + currentCameraIndex;
        if (e.code == "KeyV") {
            location.hash =
                "#" +
                JSON.stringify(
                    viewMatrix.map((k) => Math.round(k * 100) / 100),
                );
            camid.innerText = "";
        } else if (e.code === "KeyP") {
            carousel = true;
            camid.innerText = "";
        }
    });
    window.addEventListener("keyup", (e) => {
        activeKeys = activeKeys.filter((k) => k !== e.code);
    });
    window.addEventListener("blur", () => {
        activeKeys = [];
    });

    window.addEventListener(
        "wheel",
        (e) => {
            carousel = false;
            e.preventDefault();
            const lineHeight = 10;
            const scale =
                e.deltaMode == 1
                    ? lineHeight
                    : e.deltaMode == 2
                        ? innerHeight
                        : 1;
            let inv = invert4(viewMatrix);
            if (e.shiftKey) {
                inv = translate4(
                    inv,
                    (e.deltaX * scale) / innerWidth,
                    (e.deltaY * scale) / innerHeight,
                    0,
                );
            } else if (e.ctrlKey || e.metaKey) {
                // inv = rotate4(inv,  (e.deltaX * scale) / innerWidth,  0, 0, 1);
                // inv = translate4(inv,  0, (e.deltaY * scale) / innerHeight, 0);
                // let preY = inv[13];
                inv = translate4(
                    inv,
                    0,
                    0,
                    (-10 * (e.deltaY * scale)) / innerHeight,
                );
                // inv[13] = preY;
            } else {
                let d = 4;
                inv = translate4(inv, 0, 0, d);
                inv = rotate4(inv, -(e.deltaX * scale) / innerWidth, 0, 1, 0);
                inv = rotate4(inv, (e.deltaY * scale) / innerHeight, 1, 0, 0);
                inv = translate4(inv, 0, 0, -d);
            }

            viewMatrix = invert4(inv);
        },
        { passive: false },
    );

    let startX, startY, down;
    canvas.addEventListener("mousedown", (e) => {
        carousel = false;
        e.preventDefault();
        startX = e.clientX;
        startY = e.clientY;
        down = e.ctrlKey || e.metaKey ? 2 : 1;
    });
    canvas.addEventListener("contextmenu", (e) => {
        carousel = false;
        e.preventDefault();
        startX = e.clientX;
        startY = e.clientY;
        down = 2;
    });

    canvas.addEventListener("mousemove", (e) => {
        e.preventDefault();
        if (down == 1) {
            let inv = invert4(viewMatrix);
            let dx = (5 * (e.clientX - startX)) / innerWidth;
            let dy = (5 * (e.clientY - startY)) / innerHeight;
            let d = 4;

            inv = translate4(inv, 0, 0, d);
            inv = rotate4(inv, dx, 0, 1, 0);
            inv = rotate4(inv, -dy, 1, 0, 0);
            inv = translate4(inv, 0, 0, -d);
            // let postAngle = Math.atan2(inv[0], inv[10])
            // inv = rotate4(inv, postAngle - preAngle, 0, 0, 1)
            // console.log(postAngle)
            viewMatrix = invert4(inv);

            startX = e.clientX;
            startY = e.clientY;
        } else if (down == 2) {
            let inv = invert4(viewMatrix);
            // inv = rotateY(inv, );
            // let preY = inv[13];
            inv = translate4(
                inv,
                (-10 * (e.clientX - startX)) / innerWidth,
                0,
                (10 * (e.clientY - startY)) / innerHeight,
            );
            // inv[13] = preY;
            viewMatrix = invert4(inv);

            startX = e.clientX;
            startY = e.clientY;
        }
    });
    canvas.addEventListener("mouseup", (e) => {
        e.preventDefault();
        down = false;
        startX = 0;
        startY = 0;
    });

    let altX = 0,
        altY = 0;
    canvas.addEventListener(
        "touchstart",
        (e) => {
            e.preventDefault();
            if (e.touches.length === 1) {
                carousel = false;
                startX = e.touches[0].clientX;
                startY = e.touches[0].clientY;
                down = 1;
            } else if (e.touches.length === 2) {
                // console.log('beep')
                carousel = false;
                startX = e.touches[0].clientX;
                altX = e.touches[1].clientX;
                startY = e.touches[0].clientY;
                altY = e.touches[1].clientY;
                down = 1;
            }
        },
        { passive: false },
    );
    canvas.addEventListener(
        "touchmove",
        (e) => {
            e.preventDefault();
            if (e.touches.length === 1 && down) {
                let inv = invert4(viewMatrix);
                let dx = (4 * (e.touches[0].clientX - startX)) / innerWidth;
                let dy = (4 * (e.touches[0].clientY - startY)) / innerHeight;

                let d = 4;
                inv = translate4(inv, 0, 0, d);
                // inv = translate4(inv,  -x, -y, -z);
                // inv = translate4(inv,  x, y, z);
                inv = rotate4(inv, dx, 0, 1, 0);
                inv = rotate4(inv, -dy, 1, 0, 0);
                inv = translate4(inv, 0, 0, -d);

                viewMatrix = invert4(inv);

                startX = e.touches[0].clientX;
                startY = e.touches[0].clientY;
            } else if (e.touches.length === 2) {
                // alert('beep')
                const dtheta =
                    Math.atan2(startY - altY, startX - altX) -
                    Math.atan2(
                        e.touches[0].clientY - e.touches[1].clientY,
                        e.touches[0].clientX - e.touches[1].clientX,
                    );
                const dscale =
                    Math.hypot(startX - altX, startY - altY) /
                    Math.hypot(
                        e.touches[0].clientX - e.touches[1].clientX,
                        e.touches[0].clientY - e.touches[1].clientY,
                    );
                const dx =
                    (e.touches[0].clientX +
                        e.touches[1].clientX -
                        (startX + altX)) /
                    2;
                const dy =
                    (e.touches[0].clientY +
                        e.touches[1].clientY -
                        (startY + altY)) /
                    2;
                let inv = invert4(viewMatrix);
                // inv = translate4(inv,  0, 0, d);
                inv = rotate4(inv, dtheta, 0, 0, 1);

                inv = translate4(inv, -dx / innerWidth, -dy / innerHeight, 0);

                // let preY = inv[13];
                inv = translate4(inv, 0, 0, 3 * (1 - dscale));
                // inv[13] = preY;

                viewMatrix = invert4(inv);

                startX = e.touches[0].clientX;
                altX = e.touches[1].clientX;
                startY = e.touches[0].clientY;
                altY = e.touches[1].clientY;
            }
        },
        { passive: false },
    );
    canvas.addEventListener(
        "touchend",
        (e) => {
            e.preventDefault();
            down = false;
            startX = 0;
            startY = 0;
        },
        { passive: false },
    );

    let jumpDelta = 0;
    let vertexCount = 0;

    let lastFrame = 0;
    let avgFps = 0;
    let start = 0;

    window.addEventListener("gamepadconnected", (e) => {
        const gp = navigator.getGamepads()[e.gamepad.index];
        console.log(
            `Gamepad connected at index ${gp.index}: ${gp.id}. It has ${gp.buttons.length} buttons and ${gp.axes.length} axes.`,
        );
    });
    window.addEventListener("gamepaddisconnected", (e) => {
        console.log("Gamepad disconnected");
    });

    let leftGamepadTrigger, rightGamepadTrigger;

    const frame = (now) => {
        let inv = invert4(viewMatrix);
        let shiftKey =
            activeKeys.includes("Shift") ||
            activeKeys.includes("ShiftLeft") ||
            activeKeys.includes("ShiftRight");

        if (activeKeys.includes("ArrowUp")) {
            if (shiftKey) {
                inv = translate4(inv, 0, -0.03, 0);
            } else {
                inv = translate4(inv, 0, 0, 0.1);
            }
        }
        if (activeKeys.includes("ArrowDown")) {
            if (shiftKey) {
                inv = translate4(inv, 0, 0.03, 0);
            } else {
                inv = translate4(inv, 0, 0, -0.1);
            }
        }
        if (activeKeys.includes("ArrowLeft"))
            inv = translate4(inv, -0.03, 0, 0);
        //
        if (activeKeys.includes("ArrowRight"))
            inv = translate4(inv, 0.03, 0, 0);
        // inv = rotate4(inv, 0.01, 0, 1, 0);
        if (activeKeys.includes("KeyA")) inv = rotate4(inv, -0.01, 0, 1, 0);
        if (activeKeys.includes("KeyD")) inv = rotate4(inv, 0.01, 0, 1, 0);
        if (activeKeys.includes("KeyQ")) inv = rotate4(inv, 0.01, 0, 0, 1);
        if (activeKeys.includes("KeyE")) inv = rotate4(inv, -0.01, 0, 0, 1);
        if (activeKeys.includes("KeyW")) inv = rotate4(inv, 0.005, 1, 0, 0);
        if (activeKeys.includes("KeyS")) inv = rotate4(inv, -0.005, 1, 0, 0);

        const gamepads = navigator.getGamepads ? navigator.getGamepads() : [];
        let isJumping = activeKeys.includes("Space");
        for (let gamepad of gamepads) {
            if (!gamepad) continue;

            const axisThreshold = 0.1; // Threshold to detect when the axis is intentionally moved
            const moveSpeed = 0.06;
            const rotateSpeed = 0.02;

            // Assuming the left stick controls translation (axes 0 and 1)
            if (Math.abs(gamepad.axes[0]) > axisThreshold) {
                inv = translate4(inv, moveSpeed * gamepad.axes[0], 0, 0);
                carousel = false;
            }
            if (Math.abs(gamepad.axes[1]) > axisThreshold) {
                inv = translate4(inv, 0, 0, -moveSpeed * gamepad.axes[1]);
                carousel = false;
            }
            if (gamepad.buttons[12].pressed || gamepad.buttons[13].pressed) {
                inv = translate4(
                    inv,
                    0,
                    -moveSpeed *
                    (gamepad.buttons[12].pressed -
                        gamepad.buttons[13].pressed),
                    0,
                );
                carousel = false;
            }

            if (gamepad.buttons[14].pressed || gamepad.buttons[15].pressed) {
                inv = translate4(
                    inv,
                    -moveSpeed *
                    (gamepad.buttons[14].pressed -
                        gamepad.buttons[15].pressed),
                    0,
                    0,
                );
                carousel = false;
            }

            // Assuming the right stick controls rotation (axes 2 and 3)
            if (Math.abs(gamepad.axes[2]) > axisThreshold) {
                inv = rotate4(inv, rotateSpeed * gamepad.axes[2], 0, 1, 0);
                carousel = false;
            }
            if (Math.abs(gamepad.axes[3]) > axisThreshold) {
                inv = rotate4(inv, -rotateSpeed * gamepad.axes[3], 1, 0, 0);
                carousel = false;
            }

            let tiltAxis = gamepad.buttons[6].value - gamepad.buttons[7].value;
            if (Math.abs(tiltAxis) > axisThreshold) {
                inv = rotate4(inv, rotateSpeed * tiltAxis, 0, 0, 1);
                carousel = false;
            }
            if (gamepad.buttons[4].pressed && !leftGamepadTrigger) {
                camera =
                    cameras[(cameras.indexOf(camera) + 1) % cameras.length];
                inv = invert4(getViewMatrix(camera));
                carousel = false;
            }
            if (gamepad.buttons[5].pressed && !rightGamepadTrigger) {
                camera =
                    cameras[
                    (cameras.indexOf(camera) + cameras.length - 1) %
                    cameras.length
                    ];
                inv = invert4(getViewMatrix(camera));
                carousel = false;
            }
            leftGamepadTrigger = gamepad.buttons[4].pressed;
            rightGamepadTrigger = gamepad.buttons[5].pressed;
            if (gamepad.buttons[0].pressed) {
                isJumping = true;
                carousel = false;
            }
            if (gamepad.buttons[3].pressed) {
                carousel = true;
            }
        }

        if (
            ["KeyJ", "KeyK", "KeyL", "KeyI"].some((k) => activeKeys.includes(k))
        ) {
            let d = 4;
            inv = translate4(inv, 0, 0, d);
            inv = rotate4(
                inv,
                activeKeys.includes("KeyJ")
                    ? -0.05
                    : activeKeys.includes("KeyL")
                        ? 0.05
                        : 0,
                0,
                1,
                0,
            );
            inv = rotate4(
                inv,
                activeKeys.includes("KeyI")
                    ? 0.05
                    : activeKeys.includes("KeyK")
                        ? -0.05
                        : 0,
                1,
                0,
                0,
            );
            inv = translate4(inv, 0, 0, -d);
        }

        viewMatrix = invert4(inv);

        if (carousel) {
            let inv = invert4(defaultViewMatrix);

            const t = Math.sin((Date.now() - start) / 5000);
            inv = translate4(inv, 2.5 * t, 0, 6 * (1 - Math.cos(t)));
            inv = rotate4(inv, -0.6 * t, 0, 1, 0);

            viewMatrix = invert4(inv);
        }

        if (isJumping) {
            jumpDelta = Math.min(1, jumpDelta + 0.05);
        } else {
            jumpDelta = Math.max(0, jumpDelta - 0.05);
        }

        let inv2 = invert4(viewMatrix);
        inv2 = translate4(inv2, 0, -jumpDelta, 0);
        inv2 = rotate4(inv2, -0.1 * jumpDelta, 1, 0, 0);
        let actualViewMatrix = invert4(inv2);

        const cameraPos = [
            inv2[12], 
            inv2[13], 
            inv2[14]
        ];

        gl.uniform3fv(u_camPos, cameraPos);

        const viewProj = multiply4(projectionMatrix, actualViewMatrix);
        worker.postMessage({ view: viewProj });

        const currentFps = 1000 / (now - lastFrame) || 0;
        avgFps = avgFps * 0.9 + currentFps * 0.1;

        if (vertexCount > 0) {
            document.getElementById("spinner").style.display = "none";
            gl.uniformMatrix4fv(u_view, false, actualViewMatrix);
            gl.clear(gl.COLOR_BUFFER_BIT);

            gl.activeTexture(gl.TEXTURE0);
            gl.bindTexture(gl.TEXTURE_2D, mainTexture);
            gl.activeTexture(gl.TEXTURE1);
            gl.bindTexture(gl.TEXTURE_2D, shTexture);

            const error = gl.getError();
            if (error !== gl.NO_ERROR) {
                console.error("WebGL error before draw:", error);
            }

            gl.drawArraysInstanced(gl.TRIANGLE_FAN, 0, 4, vertexCount);

            const drawError = gl.getError();
            if (drawError !== gl.NO_ERROR) {
                console.error("WebGL error after draw:", drawError);
            }
        } else {
            gl.clear(gl.COLOR_BUFFER_BIT);
            document.getElementById("spinner").style.display = "";
            start = Date.now() + 2000;
        }
        fps.innerText = Math.round(avgFps) + " fps";
        if (isNaN(currentCameraIndex)) {
            camid.innerText = "";
        }
        lastFrame = now;
        requestAnimationFrame(frame);
    };

    frame();

    const isPly = (splatData) =>
        splatData[0] == 112 &&
        splatData[1] == 108 &&
        splatData[2] == 121 &&
        splatData[3] == 10;

    const selectFile = (file) => {
        const fr = new FileReader();

        if (/\.json$/i.test(file.name)) {
            fr.onload = () => {
                cameras = JSON.parse(fr.result);
                camera = cameras[0]; // Ensure global camera updates
                viewMatrix = getViewMatrix(camera);
                projectionMatrix = getProjectionMatrix(
                    camera.fx / downsample,
                    camera.fy / downsample,
                    canvas.width,
                    canvas.height,
                );
                gl.uniformMatrix4fv(u_projection, false, projectionMatrix);
                console.log("Loaded Cameras");
            };
            fr.readAsText(file);
        } else {
            stopLoading = true;
            showProgress(0, progressText.load);
            fr.onprogress = (event) => {
                if (event.lengthComputable) {
                    showProgress((event.loaded / event.total) * 50, progressText.load);
                }
            };
            fr.onload = () => {
                splatData = new Uint8Array(fr.result);
                console.log("Loaded File length:", splatData.length);
                showProgress(50, progressText.decompose);

                if (isPly(splatData)) {
                    worker.postMessage({ ply: splatData.buffer, save: true });
                } else {
                    // Send to your custom mobile-gs decoder pipeline
                    worker.postMessage({ mobilegs: splatData.buffer, save: true });
                }
            };
            fr.readAsArrayBuffer(file);
        }
    };

    window.addEventListener("hashchange", (e) => {
        try {
            viewMatrix = JSON.parse(decodeURIComponent(location.hash.slice(1)));
            carousel = false;
        } catch (err) { }
    });

    const preventDefault = (e) => {
        e.preventDefault();
        e.stopPropagation();
    };
    document.addEventListener("dragenter", preventDefault);
    document.addEventListener("dragover", preventDefault);
    document.addEventListener("dragleave", preventDefault);
    document.addEventListener("drop", (e) => {
        e.preventDefault();
        e.stopPropagation();
        selectFile(e.dataTransfer.files[0]);
    });

   


    // A simple heuristic: Mobile-GS files usually start with a 32-bit int representing JSON length.
    // Raw .splat files don't have this structure. For safety, we'll just wait for the download to finish.
    // A simple heuristic: Mobile-GS files usually start with a 32-bit int representing JSON length.
    let bytesRead = 0;  
    let stopLoading = false;
    const chunks = []; 

    while (true) {
        const { done, value } = await reader.read();
        if (done || stopLoading) break;

        chunks.push(value);
        bytesRead += value.length;
        if (contentLength > 0) {
            showProgress((bytesRead / contentLength) * 50, progressText.load);
        } else {
            showProgress(Math.min(50, 5 + chunks.length * 2), progressText.load);
        }
    }

    if (stopLoading) return;
    showProgress(50, progressText.decompose);

    // ADD THIS LINE: Re-allocate array to the exact downloaded size
    splatData = new Uint8Array(bytesRead);
    
    let currentOffset = 0;
    for (let chunk of chunks) {
        splatData.set(chunk, currentOffset);
        currentOffset += chunk.length;
    }

    // Now post the completely assembled buffer
    if (isPly(splatData)) {
        worker.postMessage({ ply: splatData.buffer, save: false });
    } else {
        worker.postMessage({ mobilegs: splatData.buffer, save: false });
    }
}

ensureViewerDom();
main().catch((err) => {
    document.getElementById("spinner").style.display = "none";
    document.getElementById("message").innerText = err.toString();
});
