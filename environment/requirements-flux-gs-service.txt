# Recommended packages for an independent HTTP service that wraps training/.
# Install this in the same environment as environment/requirements-flux-gs.txt
# if the GPU service exposes the Provider contract directly.
-r requirements-pda-adapter.txt
python-multipart>=0.0.20,<1.0
filelock>=3.15,<4.0
